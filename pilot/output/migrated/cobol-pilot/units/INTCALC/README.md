INTCALC is a Spring Boot service-module translation of the COBOL subroutine that validates a balance and interest rate, returns an error status for invalid inputs, and otherwise computes interest as balance multiplied by rate while preserving a callable library-style API for use by other programs.

## Usage

